# Frontend

The planned frontend stack is Next.js with TypeScript.

This folder is intentionally empty during the foundation phase. The application UI will be created later when frontend implementation begins.

